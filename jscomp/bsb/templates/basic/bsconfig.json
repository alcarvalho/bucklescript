{
  "name": "${bsb:name}",
  "version": "${bsb:proj-version}",
  "sources": [
    "src"
  ],
  // "reason" : { "react-jsx" : true},
  // Uncomment above to have Reason  JSX support
  "bs-dependencies" : [
      // add your bs-dependencies here 
  ]
}